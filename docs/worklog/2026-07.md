# Work log — 2026-07

## 2026-07-12 — Claude session recovery and R1 audit

- Actors: LizBing, Codex
- Archived the recoverable pre-clear and post-clear Claude dialogue.
- Reconstructed Phase 1 through R1 Block D and independently audited continuity.
- Integrated as commit `89037c4` on `main`.
- Conclusion: R1 returned to a sound baseline through Block A–D hardening, with
  R2 semantic risks explicitly identified.

## 2026-07-12 — Three-party maintenance model established

- Actors: LizBing, Codex
- LizBing designated Codex as architecture and integration maintainer; Claude
  remains the default primary implementation agent.
- Added repository-first collaboration rules, shared project status,
  workstream contracts, handoff pointers, and per-agent entry instructions.
- Next action: prepare the R2 architecture-gates contract for LizBing's review.

## 2026-07-12 — Claude/DeepSeek onboarding opened

- Actors: LizBing, Codex; implementation owner assigned to Claude.
- LizBing manually selected DeepSeek as Claude Code's backend.
- Opened `ONBOARD-1` as a documentation-only validation of fresh-session state
  recovery, scope control, and durable handoff behavior.
- Runtime and R2 implementation are explicitly out of scope.

## 2026-07-12 — Claude/DeepSeek onboarding accepted

- Claude completed the fresh-session exercise in an isolated worktree and
  changed only its live handoff file.
- Codex review found incorrect ADR/fixture counts, dirty-state wording, and
  provider-attribution certainty; the same session corrected all findings.
- LizBing clarified that CC Switch routes Claude model aliases to the manually
  selected backend and accepted ONBOARD-1.
- Integrated Claude's handoff as `609f375`; next action is the R2 architecture
  gates contract.

## 2026-07-12 — R2 architecture gates drafted

- Actor: Codex; reviewer: LizBing.
- Proposed JMM-compatible semantics over race-free Go storage, strict unresolved
  natives, logical Unsafe offsets with U0–U4 profiles, and deterministic plus
  stress/race acceptance evidence.
- Proposed eight later implementation blocks (R2-A through R2-H).
- Runtime implementation remains unauthorized pending LizBing's G1–G4 review
  and ADR-0016 through ADR-0019.

## 2026-07-12 — G1–G4 accepted and caller assumptions corrected

- LizBing accepted protected Java semantics plus a measured
  Strict/Go-native/Hybrid study, strict native failure, logical Unsafe profiles,
  and deterministic/stress/race evidence.
- Codex drafted ADR-0016 through ADR-0019 and the R2 test plan.
- Temurin 25.0.3 bytecode and Catty probes showed Integer/Long decimal output is
  corrupted by silent Unsafe array-write stubs; Double parsing has no direct
  Unsafe edge and times out; basic HashMap has no direct Unsafe edge and reaches
  an uninitialized VM dependency.
- Public current-state docs were corrected; historical session archives were
  left unchanged as records of the earlier inference.

## 2026-07-12 — R2-GATE closed

- LizBing accepted ADR-0016 through ADR-0019; ADR-0011 is superseded.
- The JDK 25 caller graph, research probes, and R2 deterministic/stress/race
  test plan form the architecture evidence package.
- R2-GATE closed without runtime implementation. The next contract is R2-A:
  strict unresolved-native failure and audited native inventory.

=== R3 Baseline Statistics ===
Source: r3-baseline.txt (generated by run-r3-baseline.sh)
Harness: bash docs/workstreams/r3-reflection-dynamic-fixtures/run-r3-baseline.sh

Total fixture table rows: 24

Engine breakdown (grep -c on r3-baseline.txt):
  Interpreter Match:   0
  Interpreter EXIT(1): 24
  IR Match:            0
  IR EXIT(1):          24
  AOT NO-BUILD:        24

Classification: Interpreter 0/24 Match, IR 0/24 Match, AOT 24/24 NO-BUILD.
Unchanged from research baseline at acceptance anchor f685526.

Verification commands:
  grep -cE '^[A-Z][a-zA-Z]+[[:space:]]{2,}' r3-baseline.txt     # fixture rows
  awk '/ REF / && $3 == "MATCH" { n++ } END { print n+0 }' r3-baseline.txt  # Interpreter matches
  grep -c 'Interpreter (EXIT(1))' r3-baseline.txt                # Interpreter failures
  awk '/ REF / && $4 == "MATCH" { n++ } END { print n+0 }' r3-baseline.txt  # IR matches
  grep -c 'IR (EXIT(1))' r3-baseline.txt                         # IR failures
  grep -c 'AOT (NO-BUILD)' r3-baseline.txt                       # AOT build failures

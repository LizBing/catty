# Claude compatibility pointer

The project status is shared by every human and agent at
[`../docs/PROJECT_STATUS.md`](../docs/PROJECT_STATUS.md).

Claude must begin at [`../CLAUDE.md`](../CLAUDE.md), then read the active
workstream and Codex handoff linked there. Do not restore a separate status
snapshot in this file; that would create two competing sources of truth.
